- name: Generate TLS certificates on localhost
  hosts: localhost
  become: true
  connection: local
  gather_facts: false
  roles:
    - role: generate_encryption_key
      tags: [generate_encryption_key]